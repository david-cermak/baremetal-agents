# OpenAI API configuration
API_KEY=your_api_key_here
BASE_URL=https://api.openai.com/v1
MODEL=gpt-4-0125-preview

# System prompt (default, can be overridden)
SYSTEM_PROMPT="You are a helpful assistant specializing in code analysis."

# Optional: Path configurations for frequent searches
ORIGINAL_CODE_PATH=/path/to/original/code
REFACTORED_CODE_PATH=/path/to/refactored/code

# Optional: Embedding model configuration
# (Only needed if you want to use a different model than the default)
EMBEDDING_MODEL=thenlper/gte-small
